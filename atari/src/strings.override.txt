# Per-port text, applied to c128/src/strings.txt by:
#
#     python3 tools/gen_strings.py --override atari/src/strings.override.txt OUT
#
# IDS THAT ALREADY EXIST, and nothing else -- the generator owns the numbering,
# so every port builds the same strdata.h and the same STR_COUNT. Only the
# strings that name the machine differ. See mega65/src/strings.override.txt.
0	 ATARI VBXE PORT 
82	TO THE ATARI 800XL.
# THE WAY OUT IS A COLD START, as on the MEGA65 -- this program occupies
# $3000..$BFFF, which is everything between DOS and the OS ROM, so there is
# nothing to return to. plat_exit() jumps through COLDSV at $E477 and what
# comes back is whatever the boot disk brings up.
10	HIT A KEY AND THE ATARI RESTARTS.
