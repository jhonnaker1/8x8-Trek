# Per-port text, applied to c128/src/strings.txt by:
#
#     python3 tools/gen_strings.py --override amiga/src/strings.override.txt OUT
#
# IDS THAT ALREADY EXIST, and nothing else -- the generator owns the numbering,
# so every port builds the same strdata.h and the same STR_COUNT. Only the two
# strings that name the machine differ. See mega65/src/strings.override.txt.
0	 AMIGA PORT 
82	TO THE AMIGA.
# As on the X16: the shared line is the C128's, and this machine has no
# 40-column screen either. vdc_shutdown() closes the window and plat_exit()
# does nothing, so the process is returned to the shell that started it.
# Unreadable until 2026-09-11 for the same reason -- worse here, because
# closing the window also made kb_waitkey() return instantly.
10	HIT A KEY TO RETURN TO AMIGADOS.
